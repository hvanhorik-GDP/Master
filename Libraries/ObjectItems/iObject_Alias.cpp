#include "iObject_Alias.h"

#include <iostream>

// For debugging purposes - dumps the contents in human readable form
std::ostream& operator<<(std::ostream& stream, const iObject_Alias& val)
{
	stream << "std::ostream& operator<<(std::ostream& stream, const iObject_Alias& val)" << "Not Implemented" << std::endl;
	return stream;
}

