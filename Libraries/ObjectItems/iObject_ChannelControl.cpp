#include "iObject_ChannelControl.h"

// For debugging purposes - dumps the contents in human readable form
std::ostream& operator<<(std::ostream& stream, const iObject_ChannelControl& val)
{
	stream << "std::ostream& operator<<(std::ostream& stream, const iObject_ChannelControl& val)" << "Not Implemented" << std::endl;
	return stream;
}
